# Core
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.31

# LLM (install the one matching llm.provider)
anthropic>=0.40        # Claude — default
openai>=1.40           # optional alternative

# Voiceover
edge-tts>=6.1          # free TTS, no key

# Captions (word-level timestamps, runs locally)
faster-whisper>=1.0

# Web dashboard
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# ffmpeg is a SYSTEM dependency — installed via scripts/install_mac.sh (brew install ffmpeg)

# Scraper (multi-source free B-roll: parallel search + ranking)
yt-dlp>=2024.8.6          # YouTube/Vimeo/TikTok search + short-excerpt download, no key
ddgs>=6.0.0               # DuckDuckGo images/videos (free, no key) — guaranteed net
google-api-python-client>=2.0  # Google Custom Search images (official, 100/day free)

# TikTok/Instagram access (TLS impersonation — passes Cloudflare-style checks;
# used by the inspiration analyzer's tikwm mirror and yt-dlp's TikTok extractor)
curl-cffi>=0.7
TikTokApi>=6.0            # Unofficial TikTok API (free, needs Playwright)
