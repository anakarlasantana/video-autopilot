These are keyframes from the SAME short-form video ({platform} — "{title}" by {author},
{duration:.0f}s). Describe its VISUAL STYLE only — do not invent the story.

Cover in {language}:
- Cut cadence and transitions visible across the frames
- Caption style (position, size, color, animation hints)
- Color grade / filters / overall aesthetic
- Overlay elements (emoji, arrows, progress bars, watermarks)
- B-roll vs talking-head vs AI-generated imagery
- Composition: vertical framing, safe zones, subject placement

Return ONLY a compact paragraph (≤120 words), no JSON, no lists.
